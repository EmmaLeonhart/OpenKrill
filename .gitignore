# krill — Kubernetes + OpenClaw deployment

# Never commit real secrets
k8s/secret.yaml

# Node / npm
node_modules/

# Context archives (can be large; manage outside git)
*.claw

# OS
.DS_Store
Thumbs.db
